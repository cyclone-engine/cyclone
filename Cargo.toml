[package]
name = "cyclone"
version = "0.1.0"
edition = "2024"

[dependencies]

[[bin]]
name = "generate_vectors"
path = "tools/generate_vectors.rs"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "protocol_bench"
harness = false
